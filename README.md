# SuperSharedMemory
基于 Windows 命名共享内存，无外部依赖，直接包含头文件即可使用的跨进程共享内存框架(A cross-process shared memory framework based on Windows named shared memory, with no external dependencies, that can be used directly by including the header file)
